# Environment Variables untuk Render Backend Deployment
# Copy paste semua variabel ini ke Render Environment Variables

# Database Configuration (WAJIB - Ganti dengan MongoDB Atlas URL Anda)
# MONGODB_URI (Ganti <db_password> dengan password Anda):
MONGODB_URI=mongodb+srv://moneymaker-admin:<db_password>@cluster0.rwvujp3.mongodb.net/money-maker-db?retryWrites=true&w=majority&appName=Cluster0

# JWT Configuration (WAJIB)
JWT_SECRET=money-maker-super-secret-jwt-key-production-2024-very-long-and-secure
JWT_EXPIRE=7d

# Server Configuration (WAJIB)
PORT=5000
NODE_ENV=production
CLIENT_URL=https://money-maker-platform.vercel.app

# Rate Limiting (Opsional)
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX_REQUESTS=100

# Admin Configuration (Opsional)
ADMIN_EMAIL=admin@moneymaker.com
ADMIN_PASSWORD=admin123456

# PANDUAN DEPLOYMENT RENDER - LANGKAH DEMI LANGKAH:

# STEP 1: Buka dashboard.render.com
# STEP 2: Klik "New" → "Web Service"
# STEP 3: Connect GitHub repository "money-maker-platform"
# STEP 4: Isi konfigurasi berikut:
#   - Name: money-maker-backend
#   - Root Directory: . (titik saja)
#   - Build Command: npm install
#   - Start Command: npm start
#   - Instance Type: Free

# STEP 5: Environment Variables - Copy paste HANYA yang WAJIB:
# (Ganti <db_password> dengan password MongoDB Atlas Anda)

# === COPY MULAI DARI SINI ===
MONGODB_URI=mongodb+srv://moneymaker-admin:MoneyMaker2024!@cluster0.rwvujp3.mongodb.net/money-maker-db?retryWrites=true&w=majority&appName=Cluster0
JWT_SECRET=money-maker-super-secret-jwt-key-production-2024-very-long-and-secure
JWT_EXPIRE=7d
PORT=5000
NODE_ENV=production
CLIENT_URL=https://money-maker-platform.vercel.app
# === COPY SAMPAI SINI ===

# STEP 6: Klik "Create Web Service"
# STEP 7: Tunggu deployment selesai (5-10 menit)